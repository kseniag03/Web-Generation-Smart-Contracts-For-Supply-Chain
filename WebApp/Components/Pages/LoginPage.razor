@page "/login"
@rendermode InteractiveServer
@inject IHttpClientFactory ClientFactory
@inject NavigationManager NavManager
@inject IJSRuntime JS

<h3>Authentication</h3>

@if (!isAuthenticated)
{
    <div class="tab-container">
        <button class="@GetTabClass(true)" @onclick="() => SetTab(true)">Sign In</button>
        <button class="@GetTabClass(false)" @onclick="() => SetTab(false)">Sign Up</button>
    </div>

    @if (isSignIn)
    {
        <div class="auth-form">
            <input type="text" placeholder="Username" @bind="loginModel.Login" />
            <input type="password" placeholder="Password" @bind="loginModel.Password" />
            <label>
                <input type="checkbox" @bind="loginModel.RememberMe" /> Remember Me
            </label>
            <button class="auth-btn" @onclick="SignIn">Sign In</button>
        </div>
    }
    else
    {
        <div class="auth-form">
            <input type="text" placeholder="Username" @bind="registerModel.Login" />
            <input type="password" placeholder="Password" @bind="registerModel.Password" />
            <input type="email" placeholder="Email (optional)" @bind="registerModel.Email" />

            <button class="auth-btn" @onclick="SignUp">Sign Up</button>
            @if (!string.IsNullOrEmpty(registrationMessage))
            {
                <p class="error-message">@registrationMessage</p>
            }
        </div>
    }
}
else
{
    <div class="user-info">
        <h4>Welcome, @user.Login!</h4>
        <p>Role: <strong>@user.Role</strong></p>

        <button class="auth-btn" @onclick="ChangePassword">Change Password</button>

        @if (user.Role == "tester")
        {
            <button class="auth-btn" @onclick="ConnectGitHub">Connect GitHub</button>
        }

        @if (user.Role == "auditor")
        {
            <button class="auth-btn" @onclick="ConnectMetaMask">Connect MetaMask</button>
        }

        @if (string.IsNullOrEmpty(user.GitHubId))
        {
            <button class="auth-btn" @onclick="ConnectGitHub">Connect GitHub</button>
        }

        @if (string.IsNullOrEmpty(user.WalletAddress))
        {
            <button class="auth-btn" @onclick="ConnectMetaMask">Connect MetaMask</button>
        }

        @if (!string.IsNullOrEmpty(githubErrorMessage))
        {
            <p class="error-message">@githubErrorMessage</p>
        }

        <button class="auth-btn logout" @onclick="Logout">Log Out</button>
    </div>
}

@code {
    private bool isAuthenticated = false;
    private bool isSignIn = true;
    private string registrationMessage = "";
    private string? githubErrorMessage;

    private LoginModel loginModel = new();
    private RegisterModel registerModel = new();
    private UserModel user = new();

    protected override async Task OnInitializedAsync()
    {
        githubErrorMessage = null;

        var uri = NavManager.ToAbsoluteUri(NavManager.Uri);

        if (QueryHelpers.ParseQuery(uri.Query).TryGetValue("errorMessage", out var errorMsg))
        {
            githubErrorMessage = errorMsg;
        }

        try
        {
            var client = ClientFactory.CreateClient("with-cookies");
            var response = await client.GetAsync("/api/auth/me");

            if (response.IsSuccessStatusCode)
            {
                var userModel = await response.Content.ReadFromJsonAsync<UserModel>();

                if (userModel is null)
                {
                    return;
                }

                user = userModel;

                isAuthenticated = true;
            }
            else
            {
                // Если пришел 401, значит пользователь не авторизован
                isAuthenticated = false;
            }
        }
        catch
        {
            // На всякий случай, вдруг сервер вообще недоступен
            isAuthenticated = false;
        }
    }

    private void SetTab(bool signIn)
    {
        isSignIn = signIn;
        registrationMessage = "";
    }

    private string GetTabClass(bool signInTab) => isSignIn == signInTab ? "tab active" : "tab";

    private async Task SignIn()
    {
        var fullUrl = "/api/auth/login";
        var client = ClientFactory.CreateClient("with-cookies");
        var response = await client.PostAsJsonAsync(fullUrl, loginModel);

        if (response.IsSuccessStatusCode)
        {
            user = await response.Content.ReadFromJsonAsync<UserModel>() ?? user;
            isAuthenticated = true;
        }
    }

    private async Task SignUp()
    {
        var fullUrl = "/api/auth/register";
        var client = ClientFactory.CreateClient("with-cookies");
        var response = await client.PostAsJsonAsync(fullUrl, registerModel);

        if (!response.IsSuccessStatusCode)
        {
            registrationMessage = "Username already exists.";
            return;
        }

        user = await response.Content.ReadFromJsonAsync<UserModel>() ?? user;
        isAuthenticated = true;
    }

    private async Task ChangePassword()
    {
        var client = ClientFactory.CreateClient("with-cookies");
        await client.PostAsync("/api/auth/change-password", null);
    }

    private void ConnectGitHub()
    {
        // ① Browser issues GET /api/auth/link-github
        //     → Set-Cookie: .AspNetCore.Correlation.GitHub=…
        //     → 302 Location: https://github.com/login/oauth/authorize?…
        // ② Browser follows that 302 to GitHub with the cookies in place
        NavManager.NavigateTo("/api/auth/link-github", forceLoad: true);
    }

    /*
    private async Task ConnectGitHub()
    {
        var client = ClientFactory.CreateClient("with-cookies");
        var response = await client.GetAsync("/api/auth/link-github");

        if (response.StatusCode == System.Net.HttpStatusCode.Redirect)
        {
            // This is the /login/oauth/authorize?… URL that ASP.NET just issued
            var githubAuthUrl = response.Headers.Location!.ToString();

            // Now hand it over to the browser (including cookies - real GitHub page)
            NavManager.NavigateTo(githubAuthUrl, forceLoad: true);
        }
        else if (response.StatusCode == System.Net.HttpStatusCode.Unauthorized)
        {
            Console.Error.WriteLine("Not authenticated with the server yet.");
        }
        else
        {
            // something else went sideways
            var body = await response.Content.ReadAsStringAsync();
            Console.Error.WriteLine($"Unexpected {response.StatusCode}: {body}");
        }
    }*/

    private async Task ConnectMetaMask()
    {
        var accounts = await JS.InvokeAsync<string[]>("myWeb3Functions.connectMetaMask");

        if (accounts.Length > 0)
        {
            var client = ClientFactory.CreateClient("with-cookies");
            var response = await client.PostAsJsonAsync("/api/auth/link-metamask", new { Address = accounts[0] });

            if (response.IsSuccessStatusCode)
            {
                user.Role = "deployer";
            }
        }
    }

    private async Task Logout()
    {
        var client = ClientFactory.CreateClient("with-cookies");
        var response = await client.PostAsync("/api/auth/logout", null);

        if (response.IsSuccessStatusCode)
        {
            isAuthenticated = false;
            user = new UserModel();

            NavManager.NavigateTo("/login", forceLoad: true);
        }
        else
        {
            Console.WriteLine("Logout failed");
        }
    }

    private class LoginModel
    {
        public string Login { get; set; } = "";
        public string Password { get; set; } = "";
        public bool RememberMe { get; set; }
    }

    private class RegisterModel
    {
        public string Login { get; set; } = "";
        public string Password { get; set; } = "";
        public string? Email { get; set; }
    }

    private class UserModel
    {
        public string Login { get; set; } = "";
        public string Role { get; set; } = "User";
        public string? GitHubId { get; set; }
        public string? WalletAddress { get; set; }
    }
}
